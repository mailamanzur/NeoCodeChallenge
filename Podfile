platform :ios, '9.0'
inhibit_all_warnings!

target 'NeoCodeChallenge' do
  pod 'Mantle', '~> 2.1'
  pod 'AFNetworking', '~> 3.1'
  pod "SDWebImage"
  pod "MBProgressHUD"

  # Has its own copy of OCMock
  # and has access to GoogleAnalytics via the app
  # that hosts the test target

  end
